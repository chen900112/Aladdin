import { useEffect, useRef, } from 'react';
import styles from './Home.less';
console.log(styles);
const Home = (props) => {
    return <div className={styles.home} >
            <div className={styles.box}>222222</div>
    </div>
}
export default Home