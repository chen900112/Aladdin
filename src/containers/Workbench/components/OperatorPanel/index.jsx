// 场景模拟器
import { useRef, forwardRef, useImperativeHandle } from 'react';
const OperatorPanel = (props, ref) => {
  
  

    return <div>
        OperatorPanel
    </div>
}
export default forwardRef(OperatorPanel)