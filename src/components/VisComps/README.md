```javascript
    // VisComp 
    // 1.负责容器的大小
    // 2.切换组件
    // 3.事件转换

    (props)=>{
       const {
           config:{
               
           },
           styles:{
               
           }
       } = props
    }
```